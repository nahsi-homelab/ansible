__archive: "vault_{{ vault_version }}_linux_amd64.zip"
__archive_url: "{{ vault_download_url }}/vault/{{ vault_version }}/{{ __archive }}"
__archive_checksum: "{{ vault_download_url }}/vault/{{ vault_version }}/vault_{{ vault_version }}_SHA256SUMS"
__archive_dir: "/tmp/{{ __archive | splitext | first }}"
