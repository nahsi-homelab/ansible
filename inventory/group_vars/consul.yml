consul_config_server:
  server: true
  bootstrap_expect: 5

  config_entries:
    bootstrap:
      - kind: "proxy-defaults"
        name: "global"
        config:
          protocol: "http"
          envoy_prometheus_bind_addr: "0.0.0.0:9102"
