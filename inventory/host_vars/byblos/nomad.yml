nomad_configs:
  volumes:
    client:
      host_volume:
        - minio:
            path: "/var/lib/minio"
        - seaweedfs-master:
            path: "/var/lib/seaweedfs-master"
        - seaweedfs-index:
            path: "/var/lib/seaweedfs-index"

  meta:
    client:
      meta:
        minio_node_id: 4
