nomad_configs:
  public_network:
    client:
      host_network:
        - public:
            interface: "ppp0"
            reserved_ports: "443"

nomad_config: >
  {{
    nomad_config_agent |
    combine(nomad_config_server, recursive=true)
  }}
