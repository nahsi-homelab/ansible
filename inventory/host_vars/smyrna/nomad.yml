nomad_configs:
  public_network:
    client:
      host_network:
        - public:
            interface: "ppp0"

  volumes:
    client:
      host_volume:
        - home-assistant:
            path: "/var/lib/home-assistant"
        - minio:
            path: "/var/lib/minio"

  meta:
    client:
      meta:
        minio_node_id: 4
