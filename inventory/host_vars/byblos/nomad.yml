nomad_configs:
  volumes:
    client:
      host_volume:
        - minio:
            path: "/var/lib/minio"
        - mongo:
            path: "/var/lib/mongo"
        - seaweedfs-master:
            path: "/var/lib/seaweedfs-master"
        - seaweedfs-index:
            path: "/var/lib/seaweedfs-index"

  meta:
    client:
      meta:
        minio_node_id: 4
        mongo_node_id: "arbitrer"
