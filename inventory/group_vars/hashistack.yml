consul_bootstrap_config:
  server: true
  bootstrap_expect: 1

consul_config_overlay:
  ui_config:
    enabled: true

  addresses:
    http: 0.0.0.0

vault_config:
  disable_mlock: true
  ui: true

  listener:
    tcp:
      address: "0.0.0.0:8200"
      tls_disable: true

  storage:
    consul: {}
