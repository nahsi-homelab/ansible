# https://distcc.github.io/
# https://raw.githubusercontent.com/distcc/distcc/master/INSTALL
# https://wiki.gentoo.org/wiki/Distcc
# Example config:
# base_distccd:
#   port: 3632
#   # if empty will log to syslog
#   log-file: "/var/log/distccd"
#   # critical, error, warning, notice, info, debug
#   log-level: "error"
#   listen: "{{ ansible_default_ipv4.address }}"
#   N: 15 # niceness

# Clients allowed to use distccd server
base_distccd_allow: []
  # - 192.168.1.0/24
  # - 192.168.1.2
