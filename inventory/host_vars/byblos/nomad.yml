nomad_configs:
  volumes:
    client:
      host_volume:
        - minio:
            path: "/var/lib/minio"
        - nats:
            path: "/var/lib/nats"
        - seaweedfs-master:
            path: "/var/lib/seaweedfs-master"
        - seaweedfs-index:
            path: "/var/lib/seaweedfs-index"

  meta:
    client:
      meta:
        nats_index: 3
        minio_node_id: 4
