[defaults]
collections_path = ~/.ansible/collections/homelab/
roles_path = ~/.ansible/roles/homelab/
inventory = inventory

timeout = 60
gather_timeout = 10

remote_tmp = /tmp/${USER}/ansible

retry_files_enabled = False
host_key_checking = False

# https://docs.ansible.com/ansible/latest/user_guide/playbooks_error_handling.html#handlers-and-failure
force_handlers = True

# disable interpreter warning
interpreter_python = auto_silent

ask_vault_pass = True

[connection]
pipelining = True

[ssh_connection]
PreferredAuthentications = publickey

# [hashi_vault_collection]
# url = https://vault.service.consul
