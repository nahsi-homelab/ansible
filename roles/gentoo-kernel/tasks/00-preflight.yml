- name: check 'arch' variable
  assert:
    that:
      - arch is defined
      - arch is string
      - arch | length
    quiet: true

- name: check 'kernel_mask_version' variable
  assert:
    that:
      - kernel_mask_version is defined
      - kernel_mask_version is string
      - kernel_mask_version | length
    quiet: true

- name: check 'kernel_boot_device' variable
  assert:
    that:
      - kernel_boot_device is defined
      - kernel_boot_device is string
      - kernel_boot_device | length
    quiet: true
