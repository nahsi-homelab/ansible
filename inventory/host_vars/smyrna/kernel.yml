kernel_version: "5.11.17"

kernel_boot_device: "/dev/disk/by-uuid/3703-0B3F"

kernel_kconfigs:
  - X86J4105

kernel_zfsbootmenu_config:
  Global:
    ManageImages: true
    BootMountPoint: /boot/efi
    DracutConfDir: /etc/zfsbootmenu/dracut.conf.d
  Components:
    ImageDir: /boot/efi/EFI/gentoo
    Versions: 1 # this will keep two versions, possibly a bug
    Enabled: true
  Kernel:
    CommandLine: ro quiet loglevel=0

kernel_refind_menu:
  - name: "Boot default"
    cmd: "root=zfsbootmenu:POOL=main timeout=10 ro quiet loglevel=0"
  - name: "Boot to menu"
    cmd: "root=zfsbootmenu:POOL=main timeout=-1 ro quiet loglevel=0"
