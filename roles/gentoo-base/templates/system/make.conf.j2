#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}
{% set __makeconf = base_portage_makeconf_default | combine(base_portage_makeconf, recursive=True) %}
{% for key, value in __makeconf.items() %}
{{ key }}="{{ value }}"
{% endfor %}
