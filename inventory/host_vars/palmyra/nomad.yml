nomad_configs:
  volumes:
    client:
      host_volume:
        - postgres:
            path: "/var/lib/postgres"
        - minio:
            path: "/var/lib/minio"
        - redis-mail:
            path: "/var/lib/redis-mail"

  meta:
    client:
      meta:
        minio_node_id: 2
