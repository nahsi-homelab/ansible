base_syslog_configs: 
  promtail: |
    destination loki {
      syslog("localhost" transport("tcp") port(1514));
    };

    log {
      source(src);
      source(kernel);
      destination(loki);
    };
