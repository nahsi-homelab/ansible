vault_version: "1.6.1"
vault_config:
  ui: true

  listener:
    tcp:
      address: "0.0.0.0:8200"
      # tls_cert_file: "/opt/vault/certs/cert.pem"
      # tls_key_file: "/opt/vault/certs/key.pem"
      tls_disable: true

  storage:
    consul:
      address: "http://consul.service.syria.consul:8500"
