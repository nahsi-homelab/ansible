nomad_configs:
  volumes:
    client:
      host_volume:
        - minio:
            path: "/var/lib/minio"
        - nats:
            path: "/var/lib/nats"

        - ceph-mon:
            path: "/var/lib/ceph/mon"
        - ceph-mgr:
            path: "/var/lib/ceph/mgr"
        - ceph-osd-1:
            path: "/var/lib/ceph/osd-1"

  meta:
    client:
      meta:
        nats_index: 2
        minio_node_id: 2
