consul_services:
  telegraf:
    service:
      id: "telegraf"
      name: "telegraf"
      port: 9271
      check:
        id: "telegraf-health"
        name: "telegraf"
        http: "http://localhost:9270"
        interval: "20s"
        timeout: "2s"
